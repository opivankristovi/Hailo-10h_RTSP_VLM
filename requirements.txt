# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
requests>=2.31.0

# Optional dependencies for frame sender
mss>=9.0.0  # For screen capture

# Hailo dependencies (installed separately via Hailo SDK)
# hailo-platform
# hailo-tappas-core